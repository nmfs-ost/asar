% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/export_to_sis.R
\name{export_to_sis}
\alias{export_to_sis}
\title{Export key quantities to SIS via Google Drive}
\usage{
export_to_sis(sis_data_dir = getwd())
}
\arguments{
\item{sis_data_dir}{Location of the sis_assmt_template.csv and sis_ts_template.csv files.

Default: The working directory.}
}
\description{
Submit completed stock assessment data to SIS via a Google Drive folder.
}
\details{
This function acts within the following workflow:
\enumerate{
\item When a stock assessment is scheduled to conclude, SIS will generate an
attachment or prompt containing metadata and identifiers.
\item The user will open two csv files containing placeholders for all of the data required by SIS: sis_assmt_template.csv (assessment summary data) and sis_ts_template.csv (time series data). There are three ways to obtain these files:
2a. Generate the files by running \code{asar::create_blank_sis()}
2b. Locate the files in the "report" folder generated by running \code{asar::create_template()}.
2c. Run \code{stockplotr::extract_sis_data()}, which will populate the templates with data originating from a converted model results file.
\item The user will add the remaining necessary data into the csv files, ensuring that all required fields are completed.
\item Run \code{export_to_sis()}, which will format and upload this data to a specific Google Drive folder.
\item The uploaded contents will be resubmitted to SIS to finalize the record.
}
}
\examples{
\dontrun{
export_to_sis(
  sis_data_dir = getwd()
)
}

}
