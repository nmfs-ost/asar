# Wrapper function for read_DOC()
#
# Descr   Break down TORs sent my SSC or council to incorporate into template document
#         Automate sectioning within the report using this format
#
# Param   doc   TOR document name, must be placed in directory
#
#

read_TOR <- function(
    doc = NA,
    ...){
  
}